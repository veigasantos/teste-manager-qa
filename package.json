{
  "name": "qa-manager",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -n API,WEB -c cyan,magenta \"npm run dev -w @qa/api\" \"npm run dev -w @qa/web\"",
    "setup": "npm install && npm run build -w @qa/shared && npm run db:setup -w @qa/api",
    "build": "npm run build -w @qa/shared && npm run build -w @qa/api && npm run build -w @qa/web",
    "start": "npm run start -w @qa/api",
    "test": "npm run test --workspaces --if-present",
    "test:e2e": "playwright test",
    "db:seed": "npm run db:seed -w @qa/api",
    "db:deploy": "npm run db:deploy -w @qa/api",
    "backup": "npm run backup -w @qa/api"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "concurrently": "^9.2.1"
  },
  "engines": {
    "node": ">=20"
  }
}
